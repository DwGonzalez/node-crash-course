const people = ['yoshi', 'ryu', 'chun-li', 'mario'];
const ages = [20, 25, 30, 35];

//console.log(people);

/* We need to use exports in order to access the data inside here */
module.exports = {
    people, ages
};